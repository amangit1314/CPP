#include<bits/stdc++.h>
using namespace std;

int check(int age){
    if(age >= 18)
        cout<<"Person is elligible for giving vote"<<endl;
    else
        cout<<"Person is elligible for giving vote"<<endl;
}

int main(){
    int age;
    cin>>age;

    check(age);
    return 0;
}